# Knowledge Engine — Python Requirements
# Phase 3 dependency audit: removed langchain-community (unused), httpx (unused),
# pypdf2 (unmaintained → replaced by pypdf), spacy moved to optional section.

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.1
pydantic-settings==2.3.0
python-multipart==0.0.9        # file upload support

# ── Async/DB ─────────────────────────────────────────────────────────────────
asyncpg==0.29.0
aiofiles==23.2.1

# ── LLM & AI (Gemini only) ───────────────────────────────────────────────────
google-genai>=1.0.0            # Google Gemini LLM + embeddings
langchain-core>=0.2.19,<0.3
# langchain-community removed — not imported anywhere in codebase (Phase 3 audit)
langgraph==0.1.9             # Agent workflow orchestration

# ── Vector Store ─────────────────────────────────────────────────────────────
qdrant-client==1.9.1           # Qdrant vector database client
sentence-transformers==3.0.1   # Local cross-encoder reranker

# ── Knowledge Graph ──────────────────────────────────────────────────────────
neo4j==5.20.0                  # Neo4j async driver

# ── Document Processing ──────────────────────────────────────────────────────
pypdf==4.2.0                   # Replaces unmaintained PyPDF2 (deprecated 2023)
python-docx==1.1.2

# ── Text Processing ───────────────────────────────────────────────────────────
tiktoken==0.7.0                # Token counting for chunkers

# ── Observability ────────────────────────────────────────────────────────────
structlog==24.2.0
opentelemetry-api==1.24.0
opentelemetry-sdk==1.24.0
opentelemetry-exporter-otlp-proto-grpc==1.24.0
opentelemetry-instrumentation-fastapi==0.45b0
prometheus-client==0.20.0

# ── Async messaging ──────────────────────────────────────────────────────────
orjson==3.10.3
aiokafka==0.10.0
httpx==0.27.0
tenacity==8.3.0
redis==5.0.4                   # Cache layer

# ── Optional (install separately if feature is enabled) ──────────────────────
# spacy>=3.7.0                 # Industrial NER — only needed if ENABLE_SPACY_NER=true
# en-core-web-lg @ https://github.com/explosion/spacy-models/releases/...
